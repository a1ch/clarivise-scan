"""
Streamlit Community Cloud default main file (repository root).

Cloud expects `streamlit_app.py` here unless you change the app path in settings.
"""
from portal.app import run_home_page

run_home_page()
